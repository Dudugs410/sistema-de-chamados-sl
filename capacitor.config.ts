import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.salvalucro',
  appName: 'chamados-SalvaLucro',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
